# FoodBot Environment Configuration
# 
# ⚠️ WICHTIG: Diese Datei als .env kopieren und ausfüllen!
# cp .env.example .env
#
# SECRET_KEY und ADMIN_PASSWORD sind PFLICHT (keine Defaults mehr!)

# Flask Secret Key (für Session-Verschlüsselung und CSRF)
# Generiere einen neuen Key mit: python3 -c 'import secrets; print(secrets.token_hex(32))'
SECRET_KEY=

# Admin-Passwort für /admin Login
ADMIN_PASSWORD=

# Flask Environment
# development = Debug-Modus, unsichere Cookies
# production = Kein Debug, sichere HTTPS-Cookies
FLASK_ENV=production

# Base URL für externe Zugriffe (wichtig für mobile QR-Codes)
# Lokal (Entwicklung): http://localhost:5001
# Raspberry Pi im Netzwerk: http://192.168.1.100
# Mit No-IP/DynDNS: http://deinname.no-ip.org
# ACHTUNG: Ohne http:// oder https:// funktioniert es nicht!
BASE_URL=http://localhost:5001

# RFID-Reader Port (Standard: /dev/ttyUSB0)
RFID_PORT=/dev/ttyUSB0
RFID_BAUDRATE=115200

# Benachrichtigungen (optional)
NOTIFICATIONS_ENABLED=false
WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL
